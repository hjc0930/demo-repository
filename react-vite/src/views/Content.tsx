import useStores from "./useStore";

const Content = () => {
  // const { state } = useStores();
  // const { time } = state;

  console.log("Time is")

  return (
    <div style={{margin: "20px"}}>
      Time is
    </div>
  );
};

export default Content;
